services:
  instantcart-backend:
    build: .
    image: akhileshaher/instantcart-backend:latest
    container_name: instantcart-backend

    ports:
      - "9091:9091"

    env_file:
      - .env

    restart: unless-stopped